Day 1
type function
    type()
Arthimetic operators
    Add,Sub,Multi,div,floor division(//) - quotient,exponent (**), modulus (%) -remainder
Increment operator 
    num +=1 (num = num+1)
abs() round()
comparision - returns booleans
cast
Booleans
is - validates if same object in memory
False values
    False
    none
    numeric type 0
    empty sequence/mapping '',[],(),{}
Loops
    for num in nums:
        print(num)
    break
    continue
    loop in a loop
    range
    while
Lists - mutable - can modify
    negative index print(courses[-1])  slicing
    append()
    insert()
    extend()
    remove()
    pop()-remove last value
Sorting List
    reverse() - here no need to reset our variables automatically applied - altering the list in place
    sort(reverse=True)
    c=sorted(courses) - need to set to a variable
    min, max, sum
    enumerate
    for index,i in enumerate(courses,start=1):
        print(index, i)
    .join(courses)
    courses.split(',')
Tuples - immutable - cant modify ()
sets - values unordered and no duplicates {}, membership test(value is present or not) other also give, but sets are optimized for this
intersection method
difference
union
empty_list=[]
empty_list=list()
empty_list=()
empty_list=tuple()
empty_list={} xxxxx - cannot create sets like these
empty_list=set()
i['value'] - to extract value from key pair/dict

Day2
Functions
func() - reuse code without repeating itself
func(a='hi')
return - stores the ouput value of the function - can treat like a datatype
def std_info(*args,**kwargs) *args - pack into tupe, **kwargs - pack into dict

courses = ['math','sci']
info = {'name':'vij','age':22}
std_info(*courses,**info) - unpack and pass them in

Dictionaries
key : value pairs
.get('key','default value') - if key not exisit it returns none instead of error
st['phoene']=5555 - to add new key value pair
student.update[{'key':'value'}]
del - keyword to delete a key
student.keys()
stu.values()
stu.items() - to display both 

def filter_orders(*orders):
    return [i for i in orders if i['amount'] > 100] ---- list compprehension
filter_orders(*orders)

customers = [{"name":"A","orders":[100, 200]},{"name":"B","orders":[50, 70]},{"name":"c","orders":[300]}]
#for each cust total order value
#print HIGH VALUE CUSTOMER if total > 250
#use looping, function for total and condition

def get_customer_total(orders):
    return sum(customer["orders"])
def label_customer(total):
    return "HIGH VALUE CUSTOMER" if total > 250 else ""
for customer in customers:
    total = get_customer_total(customer)
    label=label_customer(total)
    print(f'Customer Name : {customer["name"]}, Total : {total}, {label}')